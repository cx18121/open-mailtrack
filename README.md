# open-mailtrack

Self-hosted email open tracking for Gmail. A tiny server records tracking pixel hits, a Chrome extension adds the pixel to mail you send from Gmail and shows open status in your Sent list, and a CLI sends tracked mail through the Gmail API from a CSV.

Work in progress. The first milestone is measuring how Gmail, Apple Mail, and Google Workspace actually fetch tracking pixels before writing any open detection rules.
